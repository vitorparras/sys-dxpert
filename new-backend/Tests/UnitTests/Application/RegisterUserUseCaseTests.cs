namespace UnitTests.Application
{
    public class RegisterUserUseCaseTests
    {
        /*  [Fact]
          public async Task ExecuteAsync_WithValidData_ReturnsUserDto()
          {
              // Arrange
              var userRepositoryMock = new Mock<IUserRepository>();
              var authServiceMock = new Mock<IAuthService>();

              userRepositoryMock.Setup(r => r.GetByEmailAsync(It.IsAny<string>())).ReturnsAsync((User)null);
              authServiceMock.Setup(s => s.HashPassword(It.IsAny<string>())).Returns("hashedPassword");
              //userRepositoryMock.Setup(r => r.CreateAsync(It.IsAny<User>())).ReturnsAsync(new User("test@example.com", "hashedPassword", Role.User));

              var useCase = new RegisterUserUseCase(userRepositoryMock.Object, authServiceMock.Object);

              // Act
             // var result = await useCase.ExecuteAsync(new RegisterUserDto { Email = "test@example.com", Password = "password" });

              // Assert
            //  Assert.NotNull(result);
           //   Assert.Equal("test@example.com", result.Email);
  //Assert.Equal(Role.User.ToString(), result.Role);
          }

          [Fact]
          public async Task ExecuteAsync_WithExistingEmail_ThrowsInvalidOperationException()
          {
              // Arrange
              var userRepositoryMock = new Mock<IUserRepository>();
              var authServiceMock = new Mock<IAuthService>();

         //     userRepositoryMock.Setup(r => r.GetByEmailAsync(It.IsAny<string>())).ReturnsAsync(new User("test@example.com", "hashedPassword", Role.User));

              var useCase = new RegisterUserUseCase(userRepositoryMock.Object, authServiceMock.Object);

              // Act & Assert
        //      await Assert.ThrowsAsync<InvalidOperationException>(() =>
           //       useCase.ExecuteAsync(new RegisterUserDto { Email = "test@example.com", Password = "password" }));
          }
      }*/
    }
}
